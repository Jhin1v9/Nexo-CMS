import styles from './App.module.css';

export function App(): React.JSX.Element {
  return (
    <main className={styles.app}>
      <h1 className="text-2xl font-bold">fixture react + vite + tailwind</h1>
    </main>
  );
}
