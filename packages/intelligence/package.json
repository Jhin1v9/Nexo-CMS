{
  "name": "@nexo/intelligence",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO ProjectScanner + ProjectModel builder + fingerprint (SPEC.md §7) — read-only, evidence-based",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@nexo/adapters": "workspace:*",
    "@nexo/shared": "workspace:*"
  }
}
