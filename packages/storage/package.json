{
  "name": "@nexo/storage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO storage: better-sqlite3 atras de Repository Pattern (SPEC.md §5)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@nexo/core": "workspace:*",
    "@nexo/shared": "workspace:*",
    "better-sqlite3": "^12.11.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0"
  }
}
