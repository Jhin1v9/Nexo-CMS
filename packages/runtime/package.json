{
  "name": "@nexo/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO runtime: filesystem scope guard, command.execute, process.list (SPEC.md §4)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@nexo/shared": "workspace:*",
    "@nexo/core": "workspace:*",
    "@nexo/security": "workspace:*"
  }
}
