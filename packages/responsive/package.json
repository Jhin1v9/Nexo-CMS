{
  "name": "@nexo/responsive",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO Responsive Lab: viewports, diagnose/stressTest/compare/snapshot via browser real (doc 09, M3-CONTRACTS)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@nexo/shared": "workspace:*",
    "@nexo/core": "workspace:*",
    "@nexo/security": "workspace:*",
    "@nexo/runtime": "workspace:*",
    "@nexo/storage": "workspace:*"
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "pixelmatch": "7.2.0",
    "pngjs": "7.0.0"
  }
}