{
  "name": "@nexo/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO Control Plane: capability registry, discovery, invoke (authz+audit) e jobs (SPEC.md §8)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@nexo/core": "workspace:*",
    "@nexo/security": "workspace:*",
    "@nexo/shared": "workspace:*",
    "@nexo/storage": "workspace:*",
    "zod": "^4.4.3"
  }
}
