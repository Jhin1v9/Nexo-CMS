{
  "name": "@nexo/editor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO Editor core: Change Object, ChangeManager, save pipeline, undo/redo, source open/save (doc 07, M3-CONTRACTS)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@nexo/shared": "workspace:*",
    "@nexo/core": "workspace:*",
    "@nexo/security": "workspace:*",
    "@nexo/runtime": "workspace:*",
    "@nexo/storage": "workspace:*",
    "@nexo/intelligence": "workspace:*",
    "@nexo/adapters": "workspace:*"
  }
}