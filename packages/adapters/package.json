{
  "name": "@nexo/adapters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO Adapter Contract + detection adapters M1 (read-only) + write-path M3 (React/TSX AST transform via TS compiler, Styling Adapters Tailwind/Plain CSS) — M3-CONTRACTS/D8",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@nexo/shared": "workspace:*",
    "typescript": "^6.0.3"
  }
}
