{
  "name": "nexo-cms",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NEXO CMS \u2014 M1 FOUNDATION (ver SPEC.md)",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "vitest run tests/e2e",
    "typecheck": "pnpm -r typecheck",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "@hono/node-server": "^1.19.0",
    "@nexo/storage": "workspace:*",
    "@types/node": "^20.19.0",
    "oxlint": "^1.78.0",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
